<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>DoodStream Video Downloader</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>Doodstream / Vide0.net Downloader</h1>
    <form action="/download" method="POST">
      <input type="text" name="url" placeholder="Tempelkan URL Doodstream..." required />
      <button type="submit">Ambil Link</button>
    </form>

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <% if (videoUrl) { %>
      <div class="result">
        <video src="<%= videoUrl %>" controls width="100%"></video>
        <br>
        <a href="<%= videoUrl %>" download class="btn">Download Video</a>
      </div>
    <% } %>
  </div>
</body>
</html>
